// Generated by CoffeeScript 1.7.1
(function() {
  var resultsTemplate;

  resultsTemplate = _.template("");

  $('#search').submit(function(event) {
    event.preventDefault();
    return $.ajax({
      url: '/lookup',
      data: $(this).serialize()
    }).done(function(response) {
      var match, msg;
      console.log(response);
      if (response.length) {
        match = response[0];
        msg = "This address is located in: " + match.name;
      } else {
        msg = "Address is not in any registered neighborhood.";
      }
      return $('#search-results').html(msg);
    }).fail(function() {
      return console.log("lookup failed");
    });
  });

}).call(this);
